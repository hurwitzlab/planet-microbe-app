{
  "name": "planet-microbe-search-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "elm make --output elm.js src/Main.elm",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "$(npm bin)/elm-live src/Main.elm --output=elm.js --open"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm": "^0.19.0",
    "elm-live": "^3.3.0"
  }
}
